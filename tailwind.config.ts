const config = {
  theme: {
    extend: {
      fontFamily: {
        sans: [
          "var(--font-raleway)",
          "Helvetica Neue",
          "Helvetica",
          "Arial",
          "sans-serif",
        ],
      },
    },
  },
};

export default config;
